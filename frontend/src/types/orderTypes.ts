export interface OrderData {
  phone: string;
  address: string;
  name: string;
	city: string;
	zipcode: string;
	delivery: string;
}

export interface DefaultValues {
	phone: string;
	address: string;
	name: string;
	zipcode: string;
}
