export interface OrderData {
  phone: string;
  address: string;
  name: string;
	city: string;
	zipcode: number;
	delivery: string;
}

export interface DefaultValues {
	phone: string;
	address: string;
	name: string;
}
