// const lang = require('../resources/lang');

// const checkPassedCartID = (req, res, next) => {
//   const { cartID } = req.body;

//   if (cartID === undefined) {
//     return res.send(404).send(lang[req.curentLang].controllers);
//   }
// };

// module.exports = checkPassedCartID;
